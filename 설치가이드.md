# EMG 팔찌 앱 — 설치 및 사용 가이드

ESP32-S3 기반 8채널 근전도(EMG) 팔찌로 제스처를 녹화하고, 개인 맞춤 모델을 학습시켜, 실시간으로 손동작을 인식하는 PyQt6 데스크톱 앱입니다.

---

## 1. 하드웨어

- ESP32-S3 팔찌, EMG 센서 8채널, BLE 통신 (기기 이름: `ESP32S3_FAST_BLE`)
- 샘플링 1200Hz, 10-bit ADC

---

## 2. 준비물

### 공통
- **Python 3.10 ~ 3.12** (3.13은 TensorFlow가 지원 안 함, 반드시 이 범위로)
- **Git**
- **arduino-cli** — 팔찌 펌웨어를 컴파일/플래시하는 데 필요

### Windows 전용
- **Microsoft Visual C++ 재배포 패키지 (x64)** — 없으면 TensorFlow import가 실패함. 최신 Windows 11에는 이미 깔려있는 경우가 많으니, 에러 날 때만 설치:
  ```powershell
  winget install Microsoft.VCRedist.2015+.x64
  ```
- **프로젝트를 영문 경로에 둘 것** — 예: `C:\emg\...`. `C:\Users\한글이름\...`처럼 경로에 한글이 섞이면 인코딩 문제로 앱이 깨집니다.

### macOS 전용
- **Homebrew**

---

## 3. 설치

### Windows

```powershell
# 1) 영문 경로에 코드 받기
git clone <repo-url> C:\emg
cd C:\emg

# 2) 한 번에 설치
setup.bat
```

`setup.bat`이 자동으로 해주는 일:
- Python 3.10~3.12 중 설치된 걸 찾아서 가상환경(`.venv`) 생성
- `requirements.txt` 패키지 설치
- arduino-cli가 PATH에 있으면 ESP32 코어까지 설치 (`C:\Arduino15`라는 영문 경로에 고정 설치되도록 처리되어 있어서, 한글 계정명 컴퓨터에서도 나중에 컴파일 에러가 안 납니다)

설치 후 아래 라이브러리는 **별도로** 설치해야 합니다 (펌웨어 컴파일에 필요):
```powershell
arduino-cli lib install "Adafruit NeoPixel" "Adafruit Unified Sensor" "Adafruit BNO055"
```

### macOS

```bash
brew install python@3.12 arduino-cli git

git clone <repo-url> ~/emg
cd ~/emg
./setup.sh
```

`setup.sh`도 `setup.bat`과 동일한 일을 합니다 (venv 생성, 패키지 설치, ESP32 코어 설치). macOS는 경로에 한글이 섞여도 문제없어서 Windows용 ASCII 경로 주의사항은 해당 없습니다.

설치 후 Adafruit 라이브러리 설치도 동일하게 필요합니다:
```bash
arduino-cli lib install "Adafruit NeoPixel" "Adafruit Unified Sensor" "Adafruit BNO055"
```

---

## 4. 실행

**Windows**
```powershell
.venv\Scripts\activate.bat
python client_app\main.py
```

**macOS**
```bash
source .venv/bin/activate
python client_app/main.py
```

---

## 5. 앱 사용 흐름 (Phase 0 ~ 5)

| Phase | 내용 | 연결 방식 |
|---|---|---|
| 0 | 캡처 펌웨어 설치 | USB |
| 1 | 캘리브레이션 (5초간 팔 힘 빼고 대기) | BLE |
| 2 | 제스처 녹화 + 모델 학습 | BLE |
| 3 | 모델을 팔찌에 설치 | USB 또는 BLE |
| 4 | 설치된 모델 검증 | BLE |
| 5 | 실시간 동작 인식 테스트 | BLE |

### 꼭 알아야 할 점

- **Phase 0과 Phase 3은 사실 같은 펌웨어입니다.** 어느 화면에서 "Install" 버튼을 눌러도 동일한 펌웨어가 구워지므로, 순서를 크게 신경 쓰지 않아도 됩니다.
- **USB로 굽는 건 최초 1회면 충분합니다.** 처음 펌웨어를 올릴 때만 USB 케이블이 필요하고, 그 이후 재학습부터는 Phase 3의 **"Send weights over BLE"** 버튼으로 몇 초 만에 무선으로 새 모델을 보낼 수 있습니다 (USB 재연결 불필요).
- 단, **최초 1회는 반드시 USB를 거쳐야 합니다.** 방금 새로 구운 펌웨어는 가중치가 비어있는 상태라, BLE로 가중치를 최소 한 번은 보내야 Phase 4/5에서 예측이 나옵니다.
- 데이터/모델 저장 위치:
  - 녹화 데이터: `data/<피험자이름>_BATCH<번호>/` (CSV 파일들)
  - 학습된 모델: `models/trained/*.keras`, `models/scalers/*.pkl`

---

## 6. 자주 발생하는 문제

| 증상 | 원인 / 해결 |
|---|---|
| `python client_app\main.py` 실행 시 그냥 "Python"만 뜨고 끝남 | 가상환경을 활성화 안 하고 실행함 → `.venv\Scripts\Activate.ps1` (Windows) / `source .venv/bin/activate` (Mac) 먼저 실행 |
| 펌웨어 컴파일 중 `cc1plus.exe: out of memory` | 컴퓨터 여유 메모리 부족. 다른 프로그램(브라우저 탭 등) 닫고 재시도 |
| "Choose bracelet"에 아무것도 안 뜸 | ① 팔찌 LED가 보라색으로 계속 깜빡이면 IMU(BNO055) 센서를 못 찾아 부팅이 안 끝난 상태 — 배선/전원 확인 ② 블루투스가 꺼져있는지 확인 ③ 다른 프로그램이 이미 연결 중일 수 있음 — 팔찌 껐다 켜보기 |
| "Send weights over BLE" 시 `Characteristic ... was not found` | 팔찌가 아직 옛날/다른 펌웨어를 돌리고 있음 → Phase 3의 "Install on bracelet"(USB)을 먼저 한 번 해야 함 |
| 학습 도중 에러 메시지 없이 로그가 뚝 끊김 | 메모리 부족으로 학습 프로세스가 강제 종료됐을 가능성이 큼 (Python 예외가 아니라 OS가 죽인 경우). 로그 위치: Windows는 `%TEMP%\phase2_training.log` |

---

## 7. 참고 문서 (레포 내, 영어)

- `README.md` — 프로젝트 개요, 요약 워크플로우
- `INSTALLATION_GUIDE.md` — 더 상세한 OS별 설치 가이드 (일부 내용은 이 문서가 최신)
- `DEVELOPER_GUIDE.md` — 코드 구조, 파이프라인, 펌웨어 내부 동작
- `USER_GUIDE.md` — 비개발자를 위한 사용법 (착용 위치, 녹화 팁 등)
